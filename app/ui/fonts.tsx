import { Inter, Lustria } from 'next/font/google';
import { Open_Sans } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const openSans = Open_Sans({ subsets: ['latin'] });
export const lusitana = Lustria({
  weight: '400',
  subsets: ['latin'],
});
